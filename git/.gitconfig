[user]
    email = rares.brezeanu.dev@gmail.com
    name = imperaluna

[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true    # use n/N to move between diff sections
    side-by-side = true  # optional but nice

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default
